apiVersion: v1
kind: Service
metadata:
  name: hello-world
  namespace: production
spec:
  selector:
    app: hello-world      ← matches label in deployment.yaml
  ports:
  - port: 80
    targetPort: 3000      ← your app runs on 3000, service maps 80→3000
  type: ClusterIP